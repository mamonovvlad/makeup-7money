export const Support = {

    getObServerKeys(entity) {
        return Object.keys(entity);
    },

    getObServerValues(entity) {
        return Object.values(entity);
    },

    getObServerEntries(entity) {
        return Object.entries(entity);
    },

    getObServerLength(entity) {
        return Object.keys(entity).length;
    },

};